<%= render 'relationships/follow_button', user: @user %>
<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-4">User_id:<%= @user.name %></h4>
    <p class="lead">email:<%= @user.email %></p>
  </div>
</div>


<ul class="nav nav-pills justify-content-center" id="pills-tab" role="tablist">
  <li class="nav-item">
    <a class="nav-link active" id="pills-home-tab-2" data-toggle="pill" href="#pills-home-2" role="tab" aria-controls="pills-home-2" aria-selected="true">
      <i class="fa fa-cubes mr-1"></i> <%= @user.name %>の過去のTweet
    </a>
  </li>
  <li class="nav-item">
    <a class="nav-link" id="pills-profile-tab-2" data-toggle="pill" href="#pills-profile-2" role="tab" aria-controls="pills-profile-2" aria-selected="false">
      <i class="fa fa-diamond mr-1"></i> <%= @user.name %>がFollowしているUesr
    </a>
  </li>
</ul>
<br>

<div class="tab-content container" id="pills-tabContent">
  <div class="tab-pane fade show active" id="pills-home-2" role="tabpanel" aria-labelledby="pills-home-tab-2">
    <% if @show_tweets.present? %>
      <% @show_tweets.reverse.each do |i| %>
        <div class="card">
          <div class="card-header">
            <h5 class="card-title"><%= i.name %>&nbsp;&nbsp;<%= i.updated_at.strftime("%Y-%m-%d %H:%M") %></h5> 
          </div>
          <div class="card-body">
            <p class="card-text"><%= i.comment %></p>
          </div>
        </div>
        <br>
      <% end %>
    <% else %>
      <p>投稿はありません。上記フォームから投稿してください</p>
    <% end %>      
  </div>
    <div class="tab-pane fade" id="pills-profile-2" role="tabpanel" aria-labelledby="pills-profile-tab-2">
      <div class="list-group">
        <% if @show_follow.present? %>
          <% @show_follow.each do |i| %>
            <%= link_to i.name , i ,:class=> "list-group-item" %>
          <% end %>
        <% else %>
          <p>他のUserが登録されればここに表示されます。</p>
        <% end %>
      </div>
    </div>
</div>